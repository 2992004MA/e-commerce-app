const LOGIN = 'login';

const HOME = 'home';

const CATEGORY = 'categories';

const FAVORITE = 'favorites';

const GET_FAVORITE = 'favorites';

const PROFILE = 'profile';

const UPDATE_PROFILE = 'update-profile';

const REGISTER = 'register';

const SEARCH = 'products/search';